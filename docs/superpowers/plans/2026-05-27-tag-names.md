# Noms a les etiquetes de color (①) — Pla d'implementació

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permetre posar nom propi a cada color d'etiqueta, editar-los des d'un diàleg al menú principal i veure'ls com a tooltip als cercles del menú contextual.

**Architecture:** Una clau gsettings `tag-names` (array de 10 cadenes) guarda els noms; un helper `Files.ColorTags` (libcore) els serveix amb fallback al nom genèric; un `TagNamesDialog` (src) els edita, llançat des de l'`AppMenu` via una acció `win.edit-tag-names`; el plugin `ctags` posa el nom com a tooltip de cada cercle.

**Tech Stack:** Vala, GTK3, Granite 6.x, GSettings, Cairo, Meson/Ninja. Sense tests unitaris → compilació + verificació manual.

**Nota sobre TDD:** No hi ha harness de tests per a aquest codi GTK/GSettings; cada tasca usa `ninja -C build` com a porta automàtica i acaba amb verificació manual.

---

## Estructura de fitxers

- **Modify:** `data/schemas/io.elementary.files.gschema.xml` — clau `tag-names`.
- **Create:** `libcore/ColorTags.vala` + **Modify** `libcore/meson.build` — helper de noms.
- **Modify:** `plugins/pantheon-files-ctags/plugin.vala` — tooltips als cercles.
- **Create:** `src/Dialogs/TagNamesDialog.vala` + **Modify** `src/meson.build` — diàleg d'edició.
- **Modify:** `src/View/Window.vala` — acció `edit-tag-names` + handler.
- **Modify:** `src/View/Widgets/AppMenu.vala` — botó "Tag Names…".

---

## Task 1: Clau gsettings `tag-names`

**Files:**
- Modify: `data/schemas/io.elementary.files.gschema.xml`

- [ ] **Step 1: Afegir la clau**

A `data/schemas/io.elementary.files.gschema.xml`, dins l'schema `io.elementary.files.preferences`, just després del tancament `</key>` de `tab-info-list` i abans de `  </schema>`, afegeix:

```xml
    <key type="as" name="tag-names">
      <default>['','','','','','','','','','']</default>
      <summary>Custom names for the colour tags</summary>
      <description>One name per colour tag (indices 0-9 map to colours 1-10). Empty entries use the generic colour name.</description>
    </key>
```

- [ ] **Step 2: Validar l'esquema**

Run: `glib-compile-schemas --dry-run /home/marc/src/files/data/schemas`
Expected: sense errors (cap sortida).

- [ ] **Step 3: Commit**

```bash
git add data/schemas/io.elementary.files.gschema.xml
git commit -m "schema: clau tag-names per als noms de les etiquetes"
```

---

## Task 2: Helper Files.ColorTags (libcore)

**Files:**
- Create: `libcore/ColorTags.vala`
- Modify: `libcore/meson.build`

- [ ] **Step 1: Crear el fitxer**

Crea `libcore/ColorTags.vala`:

```vala
/*
 * Helper for colour-tag names. Names are stored globally per user in the
 * `tag-names` gsettings key; index i (0-9) maps to colour i+1 (1-10). An empty
 * entry falls back to the generic colour name.
 */

namespace Files {
    public class ColorTags : GLib.Object {
        private static GLib.Settings? settings = null;

        private static GLib.Settings get_settings () {
            if (settings == null) {
                settings = new GLib.Settings ("io.elementary.files.preferences");
            }
            return settings;
        }

        public static string generic_name (int color) {
            switch (color) {
                case 1: return _("Blue");
                case 2: return _("Mint");
                case 3: return _("Green");
                case 4: return _("Yellow");
                case 5: return _("Orange");
                case 6: return _("Red");
                case 7: return _("Pink");
                case 8: return _("Purple");
                case 9: return _("Brown");
                case 10: return _("Slate");
                default: return "";
            }
        }

        public static string[] get_names () {
            var stored = get_settings ().get_strv ("tag-names");
            var result = new string[10];
            for (int i = 0; i < 10; i++) {
                result[i] = (i < stored.length) ? stored[i] : "";
            }
            return result;
        }

        public static void set_names (string[] names) {
            var arr = new string[10];
            for (int i = 0; i < 10; i++) {
                arr[i] = (i < names.length && names[i] != null) ? names[i] : "";
            }
            get_settings ().set_strv ("tag-names", arr);
        }

        public static string display_name (int color) {
            if (color < 1 || color > 10) {
                return "";
            }
            var custom = get_names ()[color - 1];
            return (custom != null && custom != "") ? custom : generic_name (color);
        }
    }
}
```

- [ ] **Step 2: Registrar a meson**

A `libcore/meson.build`, després de la línia `'CallWhenReady.vala',` (línia ~17), afegeix:

```meson
    'ColorTags.vala',
```

- [ ] **Step 3: Compilar**

Run: `ninja -C /home/marc/src/files/build`
Expected: meson reconfigura; compila sense errors.

- [ ] **Step 4: Commit**

```bash
git add libcore/ColorTags.vala libcore/meson.build
git commit -m "ColorTags: helper de noms d'etiquetes (gsettings + fallback genèric)"
```

---

## Task 3: Tooltips als cercles del menú contextual

**Files:**
- Modify: `plugins/pantheon-files-ctags/plugin.vala`

- [ ] **Step 1: Posar el tooltip a cada cercle**

A `plugins/pantheon-files-ctags/plugin.vala`, dins `ColorWidget`'s `construct`, substitueix:

```vala
            color_button_remove = new ColorButton ("none");
```

per:

```vala
            color_button_remove = new ColorButton ("none");
            color_button_remove.tooltip_text = _("Remove colour");
```

I substitueix el bucle d'afegir els botons de color:

```vala
            for (int i = 0; i < color_buttons.size; i++) {
                colorbox.add (color_buttons[i]);
            }
```

per:

```vala
            for (int i = 0; i < color_buttons.size; i++) {
                color_buttons[i].tooltip_text = Files.ColorTags.display_name (i + 1);
                colorbox.add (color_buttons[i]);
            }
```

- [ ] **Step 2: Compilar**

Run: `ninja -C /home/marc/src/files/build`
Expected: compila sense errors.

- [ ] **Step 3: Commit**

```bash
git add plugins/pantheon-files-ctags/plugin.vala
git commit -m "ctags: tooltip amb el nom de l'etiqueta a cada cercle"
```

---

## Task 4: Diàleg TagNamesDialog

**Files:**
- Create: `src/Dialogs/TagNamesDialog.vala`
- Modify: `src/meson.build`

- [ ] **Step 1: Crear el diàleg**

Crea `src/Dialogs/TagNamesDialog.vala`:

```vala
/*
 * Lets the user assign a custom name to each of the ten colour tags.
 * Names are persisted via Files.ColorTags (gsettings key `tag-names`).
 */

public class Files.View.TagNamesDialog : Granite.Dialog {
    private Gtk.Entry[] entries;

    public TagNamesDialog (Gtk.Window parent) {
        Object (
            transient_for: parent,
            modal: true
        );
    }

    construct {
        title = _("Tag Names");

        var grid = new Gtk.Grid () {
            column_spacing = 12,
            row_spacing = 6,
            margin = 12
        };

        var names = Files.ColorTags.get_names ();
        entries = new Gtk.Entry[10];
        for (int i = 0; i < 10; i++) {
            int color = i + 1;

            var dot = new Gtk.Image.from_surface (make_dot (color)) {
                halign = Gtk.Align.CENTER,
                valign = Gtk.Align.CENTER
            };

            var entry = new Gtk.Entry () {
                hexpand = true,
                placeholder_text = Files.ColorTags.generic_name (color),
                text = names[i]
            };
            entries[i] = entry;

            grid.attach (dot, 0, i);
            grid.attach (entry, 1, i);
        }

        get_content_area ().add (grid);
        add_button (_("Done"), Gtk.ResponseType.CLOSE);

        response.connect (() => {
            save ();
            destroy ();
        });

        show_all ();
    }

    private void save () {
        var names = new string[10];
        for (int i = 0; i < 10; i++) {
            names[i] = entries[i].text.strip ();
        }
        Files.ColorTags.set_names (names);
    }

    private Cairo.Surface make_dot (int color) {
        const int PX = 16;
        var surface = new Cairo.ImageSurface (Cairo.Format.ARGB32, PX, PX);
        var cr = new Cairo.Context (surface);
        var rgba = Gdk.RGBA ();
        rgba.parse (Files.Preferences.TAGS_COLORS[color] ?? "#000000");
        cr.arc (PX / 2.0, PX / 2.0, PX / 2.0 - 2, 0, 2 * GLib.Math.PI);
        cr.set_source_rgba (rgba.red, rgba.green, rgba.blue, 1.0);
        cr.fill ();
        surface.flush ();
        return surface;
    }
}
```

- [ ] **Step 2: Registrar a meson**

A `src/meson.build`, després de la línia `'Dialogs/AbstractPropertiesDialog.vala',` (línia ~39), afegeix:

```meson
    'Dialogs/TagNamesDialog.vala',
```

- [ ] **Step 3: Compilar**

Run: `ninja -C /home/marc/src/files/build`
Expected: compila sense errors.

- [ ] **Step 4: Commit**

```bash
git add src/Dialogs/TagNamesDialog.vala src/meson.build
git commit -m "TagNamesDialog: editor dels noms de les etiquetes"
```

---

## Task 5: Acció + botó al menú principal

**Files:**
- Modify: `src/View/Window.vala`
- Modify: `src/View/Widgets/AppMenu.vala`

- [ ] **Step 1: Registrar l'acció**

A `src/View/Window.vala`, dins `WIN_ENTRIES`, després de la línia `{"focus-sidebar", action_focus_sidebar}` (afegeix una coma a aquesta línia), afegeix l'entrada nova:

```vala
        {"focus-sidebar", action_focus_sidebar},
        {"edit-tag-names", action_edit_tag_names}
```

- [ ] **Step 2: Afegir el handler**

A `src/View/Window.vala`, just abans de `private void action_edit_path () {` (línia ~838), afegeix:

```vala
    private void action_edit_tag_names () {
        var dialog = new TagNamesDialog (this);
        dialog.present ();
    }
```

- [ ] **Step 3: Afegir el botó a l'AppMenu**

A `src/View/Widgets/AppMenu.vala`, substitueix (línia ~143-145):

```vala
        menu_box.add (informal_button);

        menu_box.show_all ();
```

per:

```vala
        menu_box.add (informal_button);

        var tag_names_button = new Gtk.ModelButton () {
            text = _("Tag Names…")
        };
        tag_names_button.action_name = "win.edit-tag-names";

        menu_box.add (new Gtk.Separator (HORIZONTAL) { margin_top = 3, margin_bottom = 3 });
        menu_box.add (tag_names_button);

        menu_box.show_all ();
```

- [ ] **Step 4: Compilar**

Run: `ninja -C /home/marc/src/files/build`
Expected: compila sense errors.

- [ ] **Step 5: Commit**

```bash
git add src/View/Window.vala src/View/Widgets/AppMenu.vala
git commit -m "Window/AppMenu: obrir l'editor de noms d'etiquetes des del menú principal"
```

---

## Task 6: Compilació, instal·lació i verificació manual

**Files:** cap (verificació).

- [ ] **Step 1: Compilar el projecte sencer**

Run: `ninja -C /home/marc/src/files/build`
Expected: build complet sense errors.

- [ ] **Step 2: Instal·lar (recompila l'esquema gsettings)**

Run: `sudo ninja -C /home/marc/src/files/build install`
Expected: instal·la a `/opt/pantheon-files`; executa `glib-compile-schemas` sobre el prefix.

- [ ] **Step 3: Llançar**

Run: `~/.local/bin/io.elementary.files &`

- [ ] **Step 4: Verificacions manuals**

1. Menú hamburguesa → "Tag Names…" obre el diàleg amb 10 files (cercle de color + camp), amb placeholders dels noms genèrics.
2. Posar "manuals de dnd" a la fila groga, "Fet".
3. Clic dret sobre un fitxer → passar el ratolí per sobre el cercle groc → tooltip "manuals de dnd".
4. Cercles sense nom propi → tooltip amb el nom genèric (Blue, Green…).
5. Cercle "none" → tooltip "Remove colour".
6. Reobrir el diàleg → el groc mostra "manuals de dnd"; tancar i reobrir Files → persisteix.

- [ ] **Step 5: Commit final si calen ajustos**

Si cal retocar (mida del cercle, etiquetes), fes-ho i commiteja.

---

## Self-review (cobertura de l'spec)

- Clau gsettings `tag-names` (`as`, 10 ranures, default buit) → Task 1. ✓
- Helper `Files.ColorTags` (generic_name/display_name/get_names/set_names) → Task 2. ✓
- Diàleg amb 10 files (punt + entry) que escriu gsettings → Task 4. ✓
- Llançament des de l'AppMenu via acció `win.edit-tag-names` → Task 5. ✓
- Tooltips als cercles (color → display_name; none → "Remove colour") → Task 3. ✓
- Fallback genèric / array curt → Task 2 (`get_names` normalitza, `display_name` fallback). ✓
- Abast: només noms; no toca render (②) ni filtre (③). ✓
- Proves manuals → Task 6. ✓
